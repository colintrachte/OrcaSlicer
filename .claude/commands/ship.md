Prepare and ship the current changes.

1. Run the project's checks if they exist (lint, then tests). Fix or report failures before continuing.
2. Stage and commit in logical chunks using Conventional Commit messages.
3. Push to the current branch's upstream.
4. Summarize what was shipped (commits and branch).

Stop and ask me before pushing if anything looks risky (force-push needed, conflicts, failing tests).
